apiVersion: batch/v1
kind: Job
metadata:
  generateName: droid-deployment-reconcile-
  namespace: __NAMESPACE__
  labels:
    app: droid-deployment-reconcile
    component: deployment-reconcile
    environment: __ENVIRONMENT__
spec:
  backoffLimit: 1
  activeDeadlineSeconds: 600
  ttlSecondsAfterFinished: 3600
  template:
    metadata:
      labels:
        app: droid-deployment-reconcile
        component: deployment-reconcile
        environment: __ENVIRONMENT__
    spec:
      restartPolicy: Never
      containers:
      - name: reconciler
        image: __IMAGE__
        imagePullPolicy: Always
        command:
        - /bin/sh
        - -c
        - |
          set -eu
          set -- python -m droid_deploy.scripts.reconcile_deployment \
            --environment "${DROID_DEPLOYMENT_ENVIRONMENT}" \
            --planes "${DROID_DEPLOYMENT_PLANES}" \
            --concurrency "${DROID_DEPLOYMENT_CONCURRENCY}" \
            --apply
          if [ -n "${DROID_DEPLOYMENT_CLIENT:-}" ]; then
            set -- "$@" --client "${DROID_DEPLOYMENT_CLIENT}"
          fi
          if [ -n "${DROID_DEPLOYMENT_ASSISTANT_ID:-}" ]; then
            set -- "$@" --assistant-id "${DROID_DEPLOYMENT_ASSISTANT_ID}"
          fi
          exec "$@"
        env:
        - name: DROID_DEPLOYMENT_ENVIRONMENT
          value: "__ENVIRONMENT__"
        - name: DROID_DEPLOYMENT_PLANES
          value: "__PLANES__"
        - name: DROID_DEPLOYMENT_CONCURRENCY
          value: "__CONCURRENCY__"
        - name: DROID_DEPLOYMENT_CLIENT
          value: "__CLIENT__"
        - name: DROID_DEPLOYMENT_ASSISTANT_ID
          value: "__ASSISTANT_ID__"
        # brain_operator/deployment.py reads these at reconcile time to
        # decide whether to activate the brain.scheduled cron scenarios.
        # Empty (the default for envs that don't pass the flags) => no
        # activation, which is the safe no-op.
        - name: BRAIN_OPERATOR_ASSISTANT_ID
          value: "__BRAIN_OPERATOR_ASSISTANT_ID__"
        - name: BRAIN_OPERATOR_TASKS_ENABLED
          value: "__BRAIN_OPERATOR_TASKS_ENABLED__"
        - name: ORCHESTRA_URL
          value: "__ORCHESTRA_URL__"
        - name: DROID_COMMS_URL
          value: "__DROID_COMMS_URL__"
        - name: ORCHESTRA_ADMIN_KEY
          valueFrom:
            secretKeyRef:
              name: droid-secrets
              key: ORCHESTRA_ADMIN_KEY
