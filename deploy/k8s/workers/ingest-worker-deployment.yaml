---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: unity-ingest-worker
  namespace: default
  labels:
    app: unity-ingest-worker
    component: pipeline-worker
spec:
  replicas: 1
  selector:
    matchLabels:
      app: unity-ingest-worker
  template:
    metadata:
      labels:
        app: unity-ingest-worker
        component: pipeline-worker
    spec:
      terminationGracePeriodSeconds: 120
      containers:
      - name: ingest-worker
        image: us-central1-docker.pkg.dev/gcp-project-runtime/unity/unity:latest
        command:
        - python
        - -m
        - unity_deploy.infra.workers.entrypoint_ingest
        resources:
          requests:
            cpu: "2"
            memory: "2Gi"
          limits:
            cpu: "4"
            memory: "4Gi"
        env:
        - name: UNITY_GCP_PIPELINE_ENVIRONMENT
          value: "production"
        - name: UNITY_GCS_ARTIFACT_BUCKET
          value: "unity-pipeline-artifacts"
        - name: ORCHESTRA_ADMIN_KEY
          valueFrom:
            secretKeyRef:
              name: unity-secrets
              key: ORCHESTRA_ADMIN_KEY
        - name: GCP_SA_KEY
          valueFrom:
            secretKeyRef:
              name: unity-secrets
              key: GCP_SA_KEY
        - name: UNIFY_KEY
          valueFrom:
            secretKeyRef:
              name: unity-secrets
              key: UNIFY_KEY
        - name: UNITY_PUBSUB_PROJECT_ID
          value: "gcp-project-runtime"
        - name: USER_ID
          valueFrom:
            secretKeyRef:
              name: unity-secrets
              key: USER_ID
        - name: ASSISTANT_ID
          valueFrom:
            secretKeyRef:
              name: unity-secrets
              key: ASSISTANT_ID
        - name: UNIFY_PROJECT_NAME
          value: "Assistants"
        livenessProbe:
          exec:
            command: ["python", "-c", "import sys; sys.exit(0)"]
          initialDelaySeconds: 30
          periodSeconds: 60
        readinessProbe:
          exec:
            command: ["python", "-c", "import sys; sys.exit(0)"]
          initialDelaySeconds: 10
          periodSeconds: 30
---
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: unity-ingest-worker-hpa
  namespace: default
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: unity-ingest-worker
  minReplicas: 0
  maxReplicas: 10
  metrics:
  - type: External
    external:
      metric:
        name: pubsub.googleapis.com|subscription|num_undelivered_messages
        selector:
          matchLabels:
            resource.labels.subscription_id: unity-ingest-sub
      target:
        type: AverageValue
        averageValue: "10"
