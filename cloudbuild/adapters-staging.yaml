steps:
  # Build Docker Image
  - name: 'gcr.io/cloud-builders/docker'
    id: 'build-image'
    args:
      - 'build'
      - '-f'
      - 'Dockerfile-adapters'
      - '-t'
      - '${_REGION}-docker.pkg.dev/${PROJECT_ID}/${_REPOSITORY}/${_SERVICE_NAME}:${SHORT_SHA}'
      - '-t'
      - '${_REGION}-docker.pkg.dev/${PROJECT_ID}/${_REPOSITORY}/${_SERVICE_NAME}:latest'
      - '.'

  # Push Docker Image (SHA Tag)
  - name: 'gcr.io/cloud-builders/docker'
    id: 'push-sha-tag'
    args: ['push', '${_REGION}-docker.pkg.dev/${PROJECT_ID}/${_REPOSITORY}/${_SERVICE_NAME}:${SHORT_SHA}']
    waitFor: ['build-image']

  # Push Docker Image (Latest Tag)
  - name: 'gcr.io/cloud-builders/docker'
    id: 'push-latest-tag'
    args: ['push', '${_REGION}-docker.pkg.dev/${PROJECT_ID}/${_REPOSITORY}/${_SERVICE_NAME}:latest']
    waitFor: ['build-image']

  # Deploy to Cloud Run
  - name: 'gcr.io/google.com/cloudsdktool/cloud-sdk:slim'
    id: 'deploy'
    entrypoint: gcloud
    args:
      - 'run'
      - 'deploy'
      - '${_SERVICE_NAME}'
      - '--image=${_REGION}-docker.pkg.dev/${PROJECT_ID}/${_REPOSITORY}/${_SERVICE_NAME}:${SHORT_SHA}'
      - '--region=${_REGION}'
      - '--platform=managed'
      - '--allow-unauthenticated'
      - '--min-instances=1'
      - '--max-instances=100'
      - '--concurrency=80'
      - '--memory=512Mi'
      - '--cpu=1'
      - '--timeout=300'
      - '--cpu-boost'
      - '--set-env-vars=PROJECT_ID=${PROJECT_ID},STAGING=true'
    waitFor: ['push-sha-tag']

  # Update Cloud Scheduler: Email Watch Renewer Staging
  - name: 'gcr.io/google.com/cloudsdktool/cloud-sdk:slim'
    id: 'update-scheduler-email-watch'
    entrypoint: bash
    args:
      - '-c'
      - |
        CLOUD_RUN_URL=$(gcloud run services describe ${_SERVICE_NAME} --region=${_REGION} --format='value(status.url)')
        gcloud scheduler jobs update http "email-watch-renewer-staging" \
          --uri "$${CLOUD_RUN_URL}/scheduled/email-watch-renewer" \
          --http-method POST \
          --location="${_REGION}" \
        || \
        gcloud scheduler jobs create http "email-watch-renewer-staging" \
          --schedule "0 0 * * *" \
          --uri "$${CLOUD_RUN_URL}/scheduled/email-watch-renewer" \
          --http-method POST \
          --time-zone "UTC" \
          --location="${_REGION}"
    waitFor: ['deploy']

  # Update Cloud Scheduler: Idle Job Creator Staging
  - name: 'gcr.io/google.com/cloudsdktool/cloud-sdk:slim'
    id: 'update-scheduler-idle-creator'
    entrypoint: bash
    args:
      - '-c'
      - |
        CLOUD_RUN_URL=$(gcloud run services describe ${_SERVICE_NAME} --region=${_REGION} --format='value(status.url)')
        gcloud scheduler jobs update http "idle-job-creator-staging" \
          --uri "$${CLOUD_RUN_URL}/scheduled/idle-job-creator" \
          --http-method POST \
          --location="${_REGION}" \
        || \
        gcloud scheduler jobs create http "idle-job-creator-staging" \
          --schedule "0 * * * *" \
          --uri "$${CLOUD_RUN_URL}/scheduled/idle-job-creator" \
          --http-method POST \
          --time-zone "UTC" \
          --location="${_REGION}"
    waitFor: ['deploy']

  # Update Cloud Scheduler: Idle Job Cleaner Staging
  - name: 'gcr.io/google.com/cloudsdktool/cloud-sdk:slim'
    id: 'update-scheduler-idle-cleaner'
    entrypoint: bash
    args:
      - '-c'
      - |
        CLOUD_RUN_URL=$(gcloud run services describe ${_SERVICE_NAME} --region=${_REGION} --format='value(status.url)')
        gcloud scheduler jobs update http "idle-job-cleaner-staging" \
          --uri "$${CLOUD_RUN_URL}/scheduled/idle-job-cleaner" \
          --http-method POST \
          --location="${_REGION}" \
        || \
        gcloud scheduler jobs create http "idle-job-cleaner-staging" \
          --schedule "10 * * * *" \
          --uri "$${CLOUD_RUN_URL}/scheduled/idle-job-cleaner" \
          --http-method POST \
          --time-zone "UTC" \
          --location="${_REGION}"
    waitFor: ['deploy']

# Options
options:
  substitutionOption: ALLOW_LOOSE
  defaultLogsBucketBehavior: REGIONAL_USER_OWNED_BUCKET

# Substitution variables
substitutions:
  _REGION: 'us-central1'
  _REPOSITORY: 'unity-adapters-repo'
  _SERVICE_NAME: 'unity-adapters-staging'

# Timeout
timeout: '1200s'

# Images to be pushed
images:
  - '${_REGION}-docker.pkg.dev/${PROJECT_ID}/${_REPOSITORY}/${_SERVICE_NAME}:${SHORT_SHA}'
  - '${_REGION}-docker.pkg.dev/${PROJECT_ID}/${_REPOSITORY}/${_SERVICE_NAME}:latest'
