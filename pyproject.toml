[build-system]
requires = ["setuptools>=68.0"]
build-backend = "setuptools.build_meta"

[project]
name = "unity-deploy"
version = "0.1.0"
description = "Enterprise deployment overlay for Unity"
requires-python = ">=3.12"
dependencies = []

[project.entry-points."unity_deploy.init"]
enterprise = "unity_deploy.hook:startup_hook"

[project.entry-points."unity.deploy_runtime"]
enterprise = "unity_deploy.runtime:register_runtime_backends"

[dependency-groups]
dev = [
    "unify-agent",
    "unify-sdk",
    "unify-llm",
    "pre-commit>=4.0.1",
    "autoflake>=2.3.1",
    "black==26.1.0",
    "pytest>=9.0.2",
    "pytest-asyncio>=1.3.0",
]

[tool.uv.sources]
unify-agent = { path = "../unity", editable = true }
unify-sdk = { path = "../unify", editable = true }
unify-llm = { path = "../unillm", editable = true }

[tool.pyright]
extraPaths = ["../unity", "../unify", "../unillm"]
venvPath = "."
venv = ".venv"
reportMissingImports = "warning"

[tool.basedpyright]
extraPaths = ["../unity", "../unify", "../unillm"]
venvPath = "."
venv = ".venv"
reportMissingImports = "warning"

[tool.setuptools.packages.find]
include = ["unity_deploy*"]

[tool.setuptools.package-data]
"unity_deploy" = ["**/*.json", "**/*.md"]
