apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: desktop-unify-wildcard
  namespace: production
spec:
  secretName: desktop-unify-wildcard-tls
  issuerRef:
    name: letsencrypt-dns
    kind: ClusterIssuer
  dnsNames:
  - "*.desktop.unify.ai"
  - "desktop.unify.ai"
