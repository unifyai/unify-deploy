# Generated by brain.scheduled.scenario — DO NOT EDIT BY HAND.
# Edit the BrainScheduledJob declarations in brain and re-run
# `brain scheduled export-scenario --deployment brain_operator`.
# TEMPLATE — empty `client`, `deployment`, and per-task
# `assistant_id` fields are substituted by ScenarioActivation
# (see unity_deploy.assistant_deployments.scenarios.loader).

scenario_id: brain_jobs_v0
name: Brain scheduled jobs (v0)
description: Generated from the brain.scheduled registry.  Re-run `brain scheduled export-scenario` after changing any BrainScheduledJob
  declaration.  Activated by the brain_operator deployment via ScenarioActivation.
client: ''
deployment: ''
integration:
  package_slug: brain_jobs
  mode: real
  required_capabilities: []
  schema_version: brain.scheduled.v0
data_targets: []
timeline: []
monitors: []
alert_routes: []
dashboards: []
tasks:
- id: crm.gmail_sync
  enabled: true
  execution_mode: offline
  schedule:
    type: cron
    cron: 0 * * * *
  target:
    assistant_id: ''
    scenario_id: brain_jobs_v0
    tick_policy: incrementing
  activation:
    entrypoint_function: ingest_gmail_mailbox
    task_name: CRM Gmail sync
    task_description: Sync active Gmail mailboxes into Data/CRM source records, transcripts, and evidence.
    visibility_policy: silent_by_default
    recurrence_hint: recurring
  delivery:
    phase: real_delivery
- id: crm.fireflies_sync
  enabled: true
  execution_mode: offline
  schedule:
    type: cron
    cron: 0 * * * *
  target:
    assistant_id: ''
    scenario_id: brain_jobs_v0
    tick_policy: incrementing
  activation:
    entrypoint_function: sync_fireflies_and_associate
    task_name: CRM Fireflies sync
    task_description: Pull Fireflies transcripts, associate meetings to pipeline accounts, and ingest CRM evidence.
    visibility_policy: silent_by_default
    recurrence_hint: recurring
  delivery:
    phase: real_delivery
- id: crm.account_digest_refresh
  enabled: true
  execution_mode: offline
  schedule:
    type: cron
    cron: 0 7 * * *
  target:
    assistant_id: ''
    scenario_id: brain_jobs_v0
    tick_policy: incrementing
  activation:
    entrypoint_function: refresh_account_digests
    task_name: CRM account digest refresh
    task_description: Refresh account summaries, buying signals, blockers, and next-action recommendations.
    visibility_policy: silent_by_default
    recurrence_hint: recurring
  delivery:
    phase: real_delivery
- id: crm.hygiene_review
  enabled: true
  execution_mode: offline
  schedule:
    type: cron
    cron: 0 8 * * *
  target:
    assistant_id: ''
    scenario_id: brain_jobs_v0
    tick_policy: incrementing
  activation:
    entrypoint_function: run_crm_hygiene_review
    task_name: CRM hygiene review
    task_description: Flag companies missing owner, stage, next action, or recent evidence.
    visibility_policy: silent_by_default
    recurrence_hint: recurring
  delivery:
    phase: real_delivery
- id: crm.pipeline_review_pack
  enabled: true
  execution_mode: offline
  schedule:
    type: cron
    cron: 0 9 * * 1
  target:
    assistant_id: ''
    scenario_id: brain_jobs_v0
    tick_policy: incrementing
  activation:
    entrypoint_function: build_pipeline_review_pack
    task_name: CRM pipeline review pack
    task_description: Generate the weekly pipeline review pack from Data/CRM tables and account knowledge.
    visibility_policy: silent_by_default
    recurrence_hint: recurring
  delivery:
    phase: real_delivery
- id: outbound.evergreen.founders-smb-v1.intake
  enabled: true
  execution_mode: offline
  schedule:
    type: cron
    cron: 0 * * * *
  target:
    assistant_id: ''
    scenario_id: brain_jobs_v0
    tick_policy: incrementing
  activation:
    entrypoint_function: run_evergreen_tick__intake
    task_name: evergreen.founders-smb-v1.intake
    task_description: 'Top up the Clay intake queue with the next founder/SMB ICP slice

      if the count of unpushed, ready-to-draft contacts in CRM drops

      below `target_backlog`. Seeds via brain.sync.clay.seed using the

      named Clay workflow.

      '
    visibility_policy: silent_by_default
    recurrence_hint: recurring
  delivery:
    phase: real_delivery
- id: outbound.evergreen.founders-smb-v1.enrich
  enabled: true
  execution_mode: offline
  schedule:
    type: cron
    cron: 0 * * * *
  target:
    assistant_id: ''
    scenario_id: brain_jobs_v0
    tick_policy: incrementing
  activation:
    entrypoint_function: run_evergreen_tick__enrich
    task_name: evergreen.founders-smb-v1.enrich
    task_description: 'Apollo-reveal contacts that Clay couldn''t enrich (email + LI URL),

      bounded by the campaign''s credit budget.

      '
    visibility_policy: silent_by_default
    recurrence_hint: recurring
  delivery:
    phase: real_delivery
- id: outbound.evergreen.founders-smb-v1.draft
  enabled: true
  execution_mode: offline
  schedule:
    type: cron
    cron: '*/30 * * * *'
  target:
    assistant_id: ''
    scenario_id: brain_jobs_v0
    tick_policy: incrementing
  activation:
    entrypoint_function: run_evergreen_tick__draft
    task_name: evergreen.founders-smb-v1.draft
    task_description: 'LLM-draft email subject + body + LinkedIn note per contact whose

      enrichment is complete. Rewrites every link through the link

      tracking service for cross-channel attribution.

      '
    visibility_policy: silent_by_default
    recurrence_hint: recurring
  delivery:
    phase: real_delivery
- id: outbound.evergreen.founders-smb-v1.capacity
  enabled: true
  execution_mode: offline
  schedule:
    type: cron
    cron: '*/5 * * * *'
  target:
    assistant_id: ''
    scenario_id: brain_jobs_v0
    tick_policy: incrementing
  activation:
    entrypoint_function: run_evergreen_tick__capacity
    task_name: evergreen.founders-smb-v1.capacity
    task_description: 'Refresh today''s per-seat sending budget snapshot, accounting for

      daily caps, warmup ramps, weekly off-days, and paused seats.

      '
    visibility_policy: silent_by_default
    recurrence_hint: recurring
  delivery:
    phase: real_delivery
- id: outbound.evergreen.founders-smb-v1.push
  enabled: true
  execution_mode: offline
  schedule:
    type: cron
    cron: '*/15 * * * *'
  target:
    assistant_id: ''
    scenario_id: brain_jobs_v0
    tick_policy: incrementing
  activation:
    entrypoint_function: run_evergreen_tick__push
    task_name: evergreen.founders-smb-v1.push
    task_description: 'For each (campaign, channel) and each sendable SenderAccount

      with remaining budget, push the next batch of ranked contacts

      to Lemlist with send_user_ids=[seat.lemlist_user_id].

      '
    visibility_policy: silent_by_default
    recurrence_hint: recurring
  delivery:
    phase: real_delivery
- id: outbound.evergreen.founders-smb-v1.activity
  enabled: true
  execution_mode: offline
  schedule:
    type: cron
    cron: 0 * * * *
  target:
    assistant_id: ''
    scenario_id: brain_jobs_v0
    tick_policy: incrementing
  activation:
    entrypoint_function: run_evergreen_tick__activity
    task_name: evergreen.founders-smb-v1.activity
    task_description: 'Pull Lemlist activities for bound campaigns into Transcripts +

      CampaignMembership counters and advance Company stages.

      '
    visibility_policy: silent_by_default
    recurrence_hint: recurring
  delivery:
    phase: real_delivery
- id: outbound.evergreen.founders-smb-v1.orchestrator
  enabled: true
  execution_mode: offline
  schedule:
    type: cron
    cron: 0 */6 * * *
  target:
    assistant_id: ''
    scenario_id: brain_jobs_v0
    tick_policy: incrementing
  activation:
    entrypoint_function: run_evergreen_tick__orchestrator
    task_name: evergreen.founders-smb-v1.orchestrator
    task_description: 'Apply PolicyV1 per company: switch to secondary case study,

      breakup, pause, or continue. Always-on mode (no manual gate).

      '
    visibility_policy: silent_by_default
    recurrence_hint: recurring
  delivery:
    phase: real_delivery
- id: outbound.evergreen.founders-smb-v1.funnel
  enabled: true
  execution_mode: offline
  schedule:
    type: cron
    cron: 0 8 * * *
  target:
    assistant_id: ''
    scenario_id: brain_jobs_v0
    tick_policy: incrementing
  activation:
    entrypoint_function: run_evergreen_tick__funnel
    task_name: evergreen.founders-smb-v1.funnel
    task_description: 'Daily funnel digest with per-channel attribution emailed to the

      campaign owner. Reads brain.outbound.funnel + LinkClicks.

      '
    visibility_policy: silent_by_default
    recurrence_hint: recurring
  delivery:
    phase: real_delivery
- id: influencers.youtube.extract
  enabled: true
  execution_mode: live
  schedule:
    type: external_trigger
    trigger_name: influencers.youtube.extract
  target:
    assistant_id: ''
    scenario_id: brain_jobs_v0
    tick_policy: incrementing
  activation:
    entrypoint_function: run_youtube_browser_extraction
    task_name: YouTube influencer email extraction
    task_description: Drain the YouTube influencer-channel list through the browser-driven About-modal email extractor. Each
      capture appends to data/influencers/youtube/browser_emails.jsonl; on completion the runner regenerates channels_outreach.csv.  Fires
      once when the external trigger 'influencers.youtube.extract' is dispatched.
    visibility_policy: silent_by_default
    recurrence_hint: trigger_only
  delivery:
    phase: real_delivery
- id: intel.hackernews.daily_digest
  enabled: true
  execution_mode: offline
  schedule:
    type: cron
    cron: 0 7 * * *
  target:
    assistant_id: ''
    scenario_id: brain_jobs_v0
    tick_policy: incrementing
  activation:
    entrypoint_function: run_hackernews_digest_to_whatsapp
    task_name: HackerNews daily digest -> WhatsApp
    task_description: Scrape news.ycombinator.com via ComputerPrimitives, summarise the top stories, and send the digest to
      the operator's WhatsApp number registered with the brain_operator assistant. Recipient resolved from the BRAIN_WHATSAPP_DEFAULT_RECIPIENT
      env var when the tick fires without an explicit recipient_phone.
    visibility_policy: silent_by_default
    recurrence_hint: daily_07_00_europe_london
  delivery:
    phase: real_delivery
metadata:
  owner: brain
  documentation: docs/operations/scheduled-jobs.md
  registry_module: brain.scheduled
  template: true
